services:
  - type: web
    name: visao360-trading
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: visao360-db
          property: connectionString
